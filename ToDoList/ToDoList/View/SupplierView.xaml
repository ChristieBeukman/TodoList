<UserControl x:Class="ToDoList.View.SupplierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoList.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}">
    <Grid>
        <StackPanel>
            <ComboBox SelectedIndex="0"
                      DisplayMemberPath="Name"
                      ItemsSource="{Binding Main.Suppliers, Source={StaticResource Locator}}"/>
            <TextBox/>
        </StackPanel>
    </Grid>
</UserControl>
